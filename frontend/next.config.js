/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
}

console.log("🛠️ NEXT CONFIG LOADED: StrictMode =", nextConfig.reactStrictMode)

module.exports = nextConfig